# CNN+BiLSTM 앙상블 성능 지표 — 발표 Q&A 정리
## 1. 모델 구조 (슬라이드 18 페이지: CNN + Bi-LSTM 앙상블 모델링)

**데이터 전처리**
같은 드래그 궤적(63스텝, x/y좌표)을 두 모델이 다르게 본다.
- CNN: 시퀀스(63,8) + 스칼라 19차원(geom12 포함)
- BiLSTM: 시퀀스(63,10) + cond22(pointer 3 + waypoint count 3 + sigma-lognormal 곡선 피팅값 4 + geom12 12)
- jitter_guard: cond22를 재사용하는 보조 탐지기. 기본 판정에는 안 쓰이는 예비용(이유는 3번 참고)

**모델링 전략 — OR-rule**
CNN이든 BiLSTM이든 하나라도 봇이라 판정하면 최종 봇 처리. 원래 후보였던 stacking(결합 로지스틱 모델)은 두 모델 의견이 갈리는 애매한 케이스에서 더 자신 있어 보이는 쪽을 과신하는 결함이 있었음. kin_lognormal 공격 2만 건 전량 재계산으로 비교한 결과 jitter=0에서 OR-rule이 stacking보다 33.93%p 더 잘 막아서 OR-rule을 기본값으로 확정.

**성능 평가 — 반드시 같이 언급해야 할 한계**
- 분류 정확도 97.5%: human 300 + function 300 + gan 300 + RL 3,072 + kin_lognormal(jitter 0/0.01/0.03) 등 총 23,972건 가중평균. **이 중 진짜 사람은 300건뿐**, 나머지는 합성 봇이라 실제 트래픽 비율과 다름.
- jitter=0 우회율 0.00% (OR-rule 기준, CNN 단독은 51.88% 누락) vs **jitter=0.01(손떨림 수준 노이즈) 우회율 11.72% — 미해결 잔여 위험**. threshold 재조정으로 해결 안 됨(margin 분석 확인). jitter_guard로 0%까지 낮출 수 있지만 정상 사용자 오탐률이 6.20%까지 튀어서 실사용 불가.
- 안전 교집합(사람+규칙기반봇+GAN봇, 25,927건) AUC 0.9999는 **쉬운 조합에서만의 값**, jitter=0.01 같은 어려운 조합과는 별개.
- 검증 처리량 25~40건/초: 실측 28.4건/초(사람 400+봇 100건, 17.58초). numpy 재구현 코드, 단일 프로세스 기준.

> jitter=0(0.00%)을 말할 때 jitter=0.01(11.72%)을 반드시 같이 언급할 것. 빼면 "다 막는다"는 오해를 주는 정직성 문제.

## 2. 메인 홈페이지 지표 (97.5% / 0.3% / 25~40건/초)

| 지표 | 근거 | 한계 / Q&A 대응 |
|---|---|---|
| 분류 정확도 97.5% | 23,972건 가중평균 | 사람 300건뿐, 실 트래픽 비율과 다른 평가셋. "실 트래픽 기준은?" → "오탐률이 더 실사용에 가까운 지표" |
| 오탐률(FPR) 0.3% | human 300건 중 오탐 1건 | 표본 작아 오탐 1건만 더 나와도 0.67%로 2배. jitter_guard 검증 때도 표본 다양성 부족으로 실제 6.20%를 재현 못한 전례 있음 |
| 검증 처리량 25~40건/초 | 실측 28.4건/초 | 아래 Q3 참고 |

> 검증 처리량 25~40건/초 — 근거 / Q&A 대비
> - 직접 벤치마크로 실측: 사람 400건 + 봇 100건(총 500건)을 단일 프로세스로 순차 처리한 결과 28.4건/초(17.58초 소요) — 주장 범위(25~40) 안에 들어옴을 확인.
> - 주의할 점: 이 벤치마크는 프로덕션에 실제로 떠 있는 torch 기반 코드가 아니라, 같은 로직을 torch 없이 numpy로 재구현한 코드로 측정한 값입니다. 단일 프로세스·단일 코어 기준이고, 네트워크/직렬화 오버헤드는 포함하지 않았습니다.
> - "실제 서버 기준으로는요?" → "방향성 확인용 벤치마크이고, 정확한 서버 스펙 기준 수치는 실제 배포 환경에 부하테스트(k6/Locust 등)를 돌려 재검증할 예정입니다."

이 "재검증할 예정입니다"는 실제로 4번 항목에서 이행함 — HTTP 엔드포인트 기준·실 배포 대상 하드웨어 기준까지 총 7번의 측정으로 같은 자릿수(10건대 후반~40건대)를 재확인했음. Q&A에서 이 질문이 나오면 "예정"이 아니라 "이미 재검증했고 결과는 4번 항목과 같다"고 답할 것.

## 3. "학습 분포 안의 적만 평가한 것 아닌가"

> 봇 탐지율 99.9~100%, AUC 0.9999는 팀이 직접 생성한 합성 봇(Function/GAN/RL)에 대한 결과다. 학습 분포 안의 적을 평가한 것 아닌가? ghost-cursor·상용 솔버 등 학습 밖 봇, 그리고 슬라이드에 언급한 '판별기와 공진화한 RL 봇'에 대한 hold-out 성능은? 이 낙관적 수치로 프로덕션 임계값을 고정했다면, 실제 트래픽에서 FPR이 튀어 정상 사용자를 차단할 위험은 어떻게 관리하나?

이 질문은 세 갈래로 나눠서, 방어하려 들기보다 "맞는 지적입니다, 여기까지는 확인했고 여기부터는 아직입니다"라고 답하는 게 제일 설득력 있다.

- **인정할 점**: 99.9~100% 탐지율/AUC 0.9999는 팀이 만든 Function/GAN/RL 봇 대상 — 모델이 잡도록 설계된 공격 계열에 대한 1차 검증이지 일반화 검증은 아님.
- **RL 봇 hold-out 이슈**: "판별기와 공진화한 RL 봇"이 최종 판별기를 상대로 학습된 마지막 세대라면 순환 검증. 제대로 하려면 세대를 나눠 마지막 세대(그래디언트 노출 없음)로 따로 재평가해야 함 — 아직 안 돼 있으면 "다음 단계로 계획 중"이라고 정직하게 답할 것.
- **ghost-cursor/상용 솔버 등 학습 밖 봇**: 아직 hold-out 테스트 없음. 다만 jitter=0.01 11.72% 우회율이 이런 부류의 취약점을 이미 부분적으로 실증하고 있으므로, 이 결과를 근거로 "이미 알고 있는 약점이고 검증 계획에 포함"이라고 답하면 회피가 아니라 신뢰를 줌.
- **프로덕션 FPR 리스크 관리**: 임계값을 한번에 강제 적용하지 않고 섀도 모드로 실트래픽 관찰 → 단계적 적용. 유형1 봇 의심 시 바로 차단이 아니라 유형2로 재확인하는 완충 구조 존재. 문의하기를 통한 사람 개입 경로. 오탐 신고 기반 주기적 재조정 루프.

## 4. 검증 처리량 실측 (2026-08-19)

같은 로직(`EnsemblePredictor`, DB 쓰기 제외)을 세 번 다른 방식으로 측정:

| 측정 방식 | 워커/코어 | 처리량 |
|---|---|---|
| 순수 함수 반복 호출 (원 벤치마크) | 단일 프로세스 | 28.4건/초 |
| 순수 함수 반복 호출 (샌드박스 재현) | 단일 프로세스 | 45.2건/초 |
| 순수 함수 반복 호출 (샌드박스) | 2코어 병렬 | 83.3건/초 (1.84배) |
| 실제 HTTP 엔드포인트 (샌드박스) | uvicorn 워커 1개 | 20~35건/초 (동시성 늘려도 그대로, 지연시간만 증가) |
| 실제 HTTP 엔드포인트 (샌드박스) | uvicorn 워커 2개 | 56~64건/초 (약 2배) |
| 실제 HTTP 엔드포인트 (실 배포 대상 하드웨어) | uvicorn 워커 1개 | 17.7~20.5건/초 (동시성 1~80명 전부 이 범위에 고정) |
| 실제 HTTP 엔드포인트 (실 배포 대상 하드웨어) | uvicorn 워커 1개, 동시성 160명까지 확장 | 처리량은 여전히 18~24건/초로 그대로. p50 지연시간만 38ms(1명) → 9.5초(160명), p95는 11.6초까지 폭증 |

**결론**: 하드웨어마다 절대값은 다르지만(10건대 후반~40건대), "프로세스 1개는 초당 수십 건 선에서 막힌다"는 포화 현상은 7번의 측정에서 전부 재현됨. 워커(=프로세스) 수를 늘리면 거의 선형(2배 워커 → 약 1.84~2배 처리량)으로 늘어나는 것도 실측 확인.
동시성 160명 구간은 대기이론(Little's Law: 동시 사용자 수 ≈ 처리량 × 평균 지연시간)이 그대로 들어맞는 걸 보여주는 사례이기도 함 — 160명 ÷ 23.6건/초 ≈ 6.8초로, 실측 p50 9.5초와 같은 자릿수. 즉 동시 요청을 아무리 늘려도 "처리량"은 프로세스 상한에 묶여 있고, 늘어난 사용자는 전부 대기시간으로 흡수된다는 걸 숫자로 보여주는 좋은 예시 — 티켓팅 Q&A에서 "동시 접속자 수와 필요한 TPS는 다른 개념"이라는 주장의 근거로 쓸 수 있음.
남은 과제: DB 쓰기(usage_daily_stats, captcha_verifications insert) 포함한 실측, k6로 운영/스테이징 엔드포인트 직접 테스트.

## 5. "티켓팅 오픈 순간 초당 수천~수만 건은 어떻게 감당하나"

워커 1개 20~30건/초 기준, 초당 1만 건이면 이론상 워커 300~500개, 초당 5만 건이면 1,700~2,500개가 동시에 필요 — 단순 파드 증설만으로는 비현실적인 규모. 세 가지 축으로 답한다.

1. **유입 속도 자체를 통제**: 실제 대형 티켓팅 사이트들도 오픈 순간 전체를 결제 페이지로 바로 들여보내지 않고 가상 대기실(waiting room)로 걸러 정해진 속도로만 내보낸다. CAPTCHA는 보통 대기실 통과 이후 예매 버튼 시점에 걸리므로, 검증 계층이 받는 실제 TPS는 "동시 접속자 수"가 아니라 "대기실 배출 속도"로 정해진다. 이 배출 속도를 검증 계층의 실측 용량에 맞춰 설계하면 격차 자체가 줄어든다.
2. **필요한 만큼 사전 수평 확장**: 오픈 시각을 미리 아니까, 오픈 몇 분 전부터 파드 수를 미리 늘려뒀다가 끝나면 줄이는 방식(짧은 시간만 비용 지출). 워커 2배 → 처리량 약 2배라는 선형성은 확인했지만, 수백 파드 규모·클러스터 노드 오토스케일링까지 포함한 실측은 아직 안 함 — 다음 과제로 명시.
3. **코어당 처리량 자체 최적화**: 병목으로 추정되는 sigma-lognormal 곡선 피팅(scipy curve_fit)을 더 가벼운 근사식이나 배치 처리로 바꾸면 파드를 안 늘려도 상한을 올릴 여지가 있음 — 아직 검토 단계, 구현/실측 전.

> "지금 이대로 수만 TPS를 받을 수 있다"보다는 "유입 속도를 설계로 조절하고, 필요한 만큼은 사전 수평 확장하며, 장기적으로 처리량 자체도 최적화한다"는 전략으로 답하는 것이 정직하고 방어력 있음.